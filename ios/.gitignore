# Xcode / SwiftPM build artifacts — the .xcodeproj itself IS committed
# (it is generated from project.yml but checked in so no tooling is needed to open it).
.DS_Store
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
CorePropKit/.build/
CorePropKit/.swiftpm/
.swiftpm/
Package.resolved

# Xcode project user-specific bits (keep the shared project + shared scheme)
CoreProp.xcodeproj/xcuserdata/
CoreProp.xcodeproj/project.xcworkspace/xcuserdata/
CoreProp.xcodeproj/project.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
